'use babel';

import XBrowserPrototypeView from '../lib/x-browser-prototype-view';

describe('XBrowserPrototypeView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
